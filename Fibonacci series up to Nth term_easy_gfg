class Solution:
    def series(self, n):
        arr = [0, 1]
        for i in range(2, N+1):
            next_term = arr[i-1] + arr[i-2]
            arr.append(next_term % (10**9 + 7))
        return arr
